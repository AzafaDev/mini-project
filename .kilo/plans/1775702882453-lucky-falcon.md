# Plan: Fix Bugs di Backend

## Status
- Bug pertama sudah di-fix: `if (!events)` → `if (events.length === 0)` ✅

## Bugs yang Ditemukan (LENGKAP)

### 1. getMyEvents throws error saat tidak ada event (MEDIUM)
**File:** `backend/src/modules/events/event.service.ts:241`
**Masalah:** Throwing error saat array kosong adalah perilaku yang salah. User tanpa event adalah state valid.
**Fix:** Hapus baris 241, return `events` langsung tanpa pengecekan.

### 2. Auth middleware salah status code (LOW)
**File:** `backend/src/modules/auth/auth.middleware.ts:42,80,98`
**Masalah:** JWT error return 400, harusnya 401 untuk auth failures.
**Fix:** Ganti `res.status(400)` → `res.status(401)` di 3 lokasi catch blocks.

### 3. Unreachable null check di auth.service.ts (LOW)
**File:** `backend/src/modules/auth/auth.service.ts:294`
**Masalah:** `prisma.user.update()` throws error jika record tidak ada, tidak pernah return null.
**Fix:** Hapus baris `if (!user) throw new AppError("User not found", 404);`

### 4. deleteEvent controller tidak handle error (LOW)
**File:** `backend/src/modules/events/event.controller.ts:234-246`
**Masalah:** `deleteEvent` controller tidak check hasil dari service, jika error tidak terdeteksi.
**Fix:** Wrap dalam try-catch atau check result.

### 5. getMyEvents controller tidak handle error (LOW)
**File:** `backend/src/modules/events/event.controller.ts:248-257`
**Masalah:** Sama seperti deleteEvent, tidak ada error handling.
**Fix:** Wrap dalam try-catch atau check result.

## Implementation (3 bugs utama)

1. **Fix event.service.ts:241** - Hapus `if (events.length === 0) throw new AppError(...)` ⬅️ TERPENTING
2. **Fix auth.middleware.ts:42,80,98** - Ganti status 400 → 401 di catch blocks
3. **Fix auth.service.ts:294** - Hapus unreachable null check

Mau saya lanjut fix semua bugs ini?
